// email service for FlowForge AI
export class emailService {
  async init() { return true }
  async dispose() { return true }
  isReady() { return true }
}
export const emailService = new emailService()
